public class RandomContactFactory {
    public static List <Contact> generateRandomContacts(Integer intr, string str){
        
        List<contact> con = new List <contact>();
    for(Integer i=0;i<intr;i++) {
            Contact c = new Contact(FirstName='Test ' + i);
						new Contact (LastName = 'str');
        con.add(c);
        
        
        
        
        
    }
return con;
}
}